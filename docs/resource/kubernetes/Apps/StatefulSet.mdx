---
title: StatefulSet
sidebar_position: 0
tags: [Kubernetes, 리소스, Apps, StatefulSet]
---

<hr />
<br />

## 리소스 아이콘

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/kubernetesImg/apps-stateful_set.webp"
    }
/>

## 리소스 개요

Pod 집합의 배포 및 확장을 관리하고 이러한 Pod의 순서와 고유성에 대한 보장을 제공하는 쿠버네티스 리소스입니다.

## 연관 리소스

### 상위 리소스

-   [**Namespace**](https://docs.cstudio.app/docs/resource/kubernetes/Core/Namespace)

### 연결 리소스

-   [**Config Map**](https://docs.cstudio.app/docs/resource/kubernetes/Config/ConfigMap)
-   [**Horizontal Pod Autoscaler**](https://docs.cstudio.app/docs/resource/kubernetes/AutoScaling/HorizontalPodAutoscaler)
-   [**Persistent Volume Claim**](https://docs.cstudio.app/docs/resource/kubernetes/Core/PersistentVolumeClaim)
-   [**Service**](https://docs.cstudio.app/docs/resource/kubernetes/Core/Service)

## 리소스 설정 값

#### 기본 설정

-   `namespace` : StatefulSet 리소스가 배포될 네임스페이스
-   `annotations` : 쿠버네티스 시스템 메타데이터 key-value
-   `labels` : StatefulSet 리소스 식별용 라벨
-   `template_labels` : 생성될 Pod에 적용되는 라벨
-   `pod_management_policy` : Pod 생성·삭제 순서 제어 방식 (`Parallel`, `OrderedReady`)
-   `revision_history_limit` : 유지할 개정 이력 수
-   `linked_service_name` : StatefulSet을 관리하는 Headless Service 이름
-   `replicas` : 유지할 Pod 복제본 수
-   `pod_security_context` : Pod 단위 보안 및 권한 설정
-   `init_containers` : 메인 컨테이너 실행 전 초기화 컨테이너 정의
-   `containers` : Pod에 포함될 컨테이너 정의
-   `volume` : Pod에서 사용할 볼륨 정의
-   `dns_policy` : Pod DNS 정책
-   `enable_service_links` : 서비스 환경 변수 자동 주입 여부
-   `host_network` : 호스트 네트워크 사용 여부
-   `image_pull_secrets` : 이미지 Pull 시 사용할 Secret 목록

---

## 참고 자료

-   [StatefulSet(Kubernetes)](https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/)
-   [StatefulSet(Terraform)](https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs/resources/stateful_set)
