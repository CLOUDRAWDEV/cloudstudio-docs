---
title: WindowsVirtualMachine
sidebar_position: 2
tags: [Azure, Compute, WindowsVirtualMachine]
---

<hr />
<br />

## 리소스 아이콘

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/azureImg/compute-windows_virtual_machine.webp"
    }
/>

## 리소스 개요

Azure WindowsServer Virtual Machine은 Microsoft의 Azure 클라우드 컴퓨팅 플랫폼에서 WindowsServer 운영 체제를 실행하는 가상 머신(VM)입니다.
Azure에서 WindowsServer 가상 머신을 실행함으로써 사용자는 선호하는 Linux 배포를 사용하면서 Microsoft 클라우드 플랫폼의 확장성, 안정성 및 글로벌 범위를 활용할 수 있습니다.

## 연관 리소스

### 상위 리소스

-   [**VirtualNetwork**](https://docs.cstudio.app/docs/resource/azure/Network/VirtualNetwork)
-   [**Subnet**](https://docs.cstudio.app/docs/resource/azure/Network/Subnet)

### 연결 리소스

-   [**AvailabilitySet**](https://docs.cstudio.app/docs/resource/azure/Compute/Availabilityset)
-   [**Disk**](https://docs.cstudio.app/docs/resource/azure/Compute/Disk)
-   [**NetworkInterface**](https://docs.cstudio.app/docs/resource/azure/Network/NetworkInterface)
-   [**Backup**](https://docs.cstudio.app/docs/resource/azure/RecoveryServices/Backup)
-   [**Metric**](https://docs.cstudio.app/docs/resource/azure/Monitor/Metric)
-   [**PrivateLoadBalancer**](https://docs.cstudio.app/docs/resource/azure/LoadBalancer/PrivateLoadBalancer)
-   [**PublicLoadBalancer**](https://docs.cstudio.app/docs/resource/azure/LoadBalancer/PublicLoadBalancer)
-   [**ApplicationGateway**](https://docs.cstudio.app/docs/resource/azure/Network/ApplicationGateway)

## 리소스 설정 값

-   `included_vnet_name` : Virtual Machine이 속하는 Virtual Network 이름

-   `linked_availability_set_name` : Virtual Machine에 적용할 Availability Set 이름

-   `size` : 워크로드 실행을 위한 Virtual Machine의 크기 설정

-   `computer_name` : Virtual Machine의 Hostname

-   `admin_username` : VM에 대한 관리자 사용자 이름

    -   1 ~ 64자

-   `admin_password` : VM에 대한 관리자 암호

    -   12 ~ 72자

-   `os_disk_caching` : 내부 OS Disk 사용을 위한 Caching 유형

    -   `ReadWrite`, `ReadOnly`, `None`

-   `storage_account_type` : 내부 OS Disk 스토리지 유형

    -   `Standard_LRS`, `StandardSSD_LRS`, `Premium_LRS`

-   `disk_size_gb` : 내부 OS Disk의 크기(GB)

    -   `32` ~ `32767`

-   `linked_network_interface_names` : Network Interface 이름 목록

-   `primary_network_interface_name` : 기본으로 사용할 Network Interface 이름

-   `tag` : 리소스를 카테고리로 분류하기 위한 태그
    -   최대 512자, `key`:`value`

### 이미지 설정 (`source_image`)

-   `source_image.type` : 사용자 이미지 ID 사용 유무

    -   `recommended`, `custom_id`, `custom_input`

-   `source_image.image_id` : VM 생성을 위해 사용하는 이미지의 ID

-   `source_image.image` : VM 생성을 위해 사용하는 이미지

-   `source_image.publisher` : VM 생성을 위해 사용하는 이미지의 공급자

-   `source_image.offer` : VM 생성을 위해 사용하는 이미지 Offer

-   `source_image.sku` : VM 생성을 위해 사용하는 이미지 SKU

### 디스크 추가 연결 (`disk_attachment`)

-   `disk_attachment.lun` : 디스크의 논리 장치 번호

-   `disk_attachment.host_caching` : Disk caching 설정

    -   `None`, `ReadOnly`, `ReadWrite`

---

## 참고 자료

-   [Azure Virtual Machines(Microsoft)](https://learn.microsoft.com/ko-kr/azure/virtual-machines/)
-   [Azure Windows Virtual Machine(Terraform)](https://registry.terraform.io/providers/hashicorp/azurerm/latest/docs/resources/windows_virtual_machine)
