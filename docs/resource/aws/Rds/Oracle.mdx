---
title: Oracle
sidebar_position: 4
tags: [AWS, 리소스, Rds, Oracle]
---

<hr />
<br />

## 리소스 아이콘

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/awsImg/rds-oracle.webp"
    }
/>

## 리소스 개요

Amazon RDS for Oracle은 클라우드에서 Oracle 배포를 손쉽게 설정, 운영 및 확장할 수 있는 완전관리형 상용 데이터베이스입니다. Amazon RDS는 프로비저닝, 백업, 소프트웨어 패치, 모니터링, 하드웨어 크기 조정 같은 시간 소모적인 데이터베이스 관리 업무를 대신 처리하므로 고객은 혁신과 애플리케이션 개발에만 집중할 수 있습니다.

## 연관 리소스

### 상위 리소스

-   [**Subnet**](https://docs.cstudio.app/docs/resource/aws/Vpc/Subnet)

### 연결 리소스

-   [**Oracle**](https://docs.cstudio.app/docs/resource/aws/Rds/Oracle)
-   [**SecurityGroup**](https://docs.cstudio.app/docs/resource/aws/Vpc/SecurityGroup)

## 리소스 설정 값

## 리소스 설정 값

#### 기본 설정

-   `database_name` : 초기 데이터베이스 이름 - `1` ~ `8`자의 영대문자 및 숫자
-   `license` : Oracle 라이선스 모델 - `bring-your-own-license`, `license-included`
-   `engine` : Oracle 엔진 유형 - `oracle-ee`, `oracle-se2`
-   `engine_version` : RDS에 사용할 Oracle 엔진 버전
-   `instance_class` : RDS DB 인스턴스 타입
-   `storage_type` : RDS 인스턴스 스토리지 타입
-   `allocated_storage` : RDS 스토리지 용량(GB) - `20` ~ `65536`
-   `max_allocated_storage` : 스토리지 자동 확장 최대 한계값
-   `iops` : DB 인스턴스가 지원하는 초당 I/O 작업 수 - `1000` ~ `40000`
-   `multi_az_enabled` : 다중 AZ 배포 활성화 여부 - `true`, `false`
-   `username` : RDS 관리자 사용자 이름
-   `password` : RDS 관리자 사용자 비밀번호
-   `publicly_accessible` : 퍼블릭 액세스 허용 여부 - `true`, `false`

#### 네트워크 설정

-   `included_subnet_names` : RDS Subnet Group에 포함될 Subnet 이름 목록
-   `subnet_group_name` : RDS에 사용할 Subnet Group 이름
-   `linked_security_group_names` : 적용할 보안 그룹 목록

#### 모니터링 설정 (`monitoring`)

-   `monitoring.performance_insights_enabled` : Performance Insights 활성화 여부 - `true`, `false`
-   `monitoring.kms_key_name` : Performance Insights에 사용할 KMS Key 이름

#### 암호화 설정 (`encryption`)

-   `encryption.storage_encrypted` : DB 인스턴스 스토리지 암호화 여부 - `true`, `false`
-   `encryption.kms_key_name` : 암호화에 사용할 KMS Key 이름

#### 백업 설정 (`backup`)

-   `backup.backup_retention_period` : 백업 보관 기간(일) - `1` ~ `35`
-   `backup.final_snapshot_enabled` : DB 삭제 전 최종 스냅샷 생성 여부 - `true`, `false`
-   `backup.final_snapshot_name` : 최종 스냅샷 이름

#### 태그

-   `tag` : 리소스를 카테고리로 분류하기 위한 태그

---

## 참고 자료

-   [Amazon RDS for Oracle 기능(Amazon RDS)(AWS)](https://aws.amazon.com/ko/rds/oracle/features/?nc=sn&loc=2&dn=1)
-   [Amazon RDS for Oracle 요금(AWS)](https://aws.amazon.com/ko/rds/oracle/pricing/)
