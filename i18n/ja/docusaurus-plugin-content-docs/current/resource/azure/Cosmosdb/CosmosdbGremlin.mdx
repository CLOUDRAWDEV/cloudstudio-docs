---
title: CosmosdbGremlin
sidebar_position: 1
tags: [Azure, CosmosdbGremlin, CosmosDB, Gremlin, Database]
---

<hr />
<br />

## リソースアイコン

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/azureImg/cosmosdb-cosmosdb_gremlin.webp"
    }
/>

## リソース概要

Azure Cosmos DB Gremlin は、グラフデータを保存およびクエリできる完全マネージド型の NoSQL データベースサービスです。  
グローバル分散、自動スケーリング、バックアップ、高可用性の機能を提供し、スケーラブルで高性能なアプリケーションの構築を可能にします。

## 関連リソース

### 上位リソース

-   [**Resource Group**](https://docs.cstudio.app/ja/docs/resource/azure/Base/ResourceGroup)

### 接続リソース

-   [**Private EndPoint**](https://docs.cstudio.app/ja/docs/resource/azure/Network/PrivateEndpoint)

## リソース設定値

#### 基本設定

-   `capabilities_list` : 使用する Cosmos DB の API 一覧
-   `enable_multiple_write_locations` : 複数リージョン書き込みを有効にするかどうか - `true`, `false`
-   `public_network_access_enabled` : パブリック IP アドレスまたはサービス エンドポイントを介したアカウントへの公開アクセスを有効にするかどうか - `true`, `false`
-   `enable_automatic_failover` : 自動フェールオーバーを有効にするかどうか - `true`, `false`
-   `virtual_network_filter_enabled` : 特定の仮想ネットワーク Subnet のみがアカウントにアクセスできるようにフィルターを有効にするかどうか - `true`, `false`
-   `network_acl_bypass_for_azure_services` : Azure サービスに対する ACL バイパスを有効にするかどうか - `true`, `false`

#### リージョン設定（`geo_location`）

-   `geo_location.failover_priority` : リージョンのフェールオーバー優先順位
-   `geo_location.zone_redundant` : 障害に備えて自動的にゾーン冗長を有効にするかどうか - `true`, `false`

#### 一貫性ポリシー（`consistency_policy`）

-   `consistency_policy.consistency_level` : データベースの可用性とパフォーマンスを制御する一貫性レベル
-   `consistency_policy.max_interval_in_seconds` : 一貫性レベルが Bounded Staleness の場合に許容される最大遅延時間（秒）
-   `consistency_policy.max_staleness_prefix` : 一貫性レベルが Bounded Staleness の場合に許容される最大 Stale リクエスト数

#### バックアップ設定（`backup`）

-   `backup.type` : バックアップポリシーの種類 - `Continuous`, `Periodic`
-   `backup.interval_in_minutes` : バックアップ実行間隔（分） - `60` ~ `1440`
-   `backup.retention_in_hours` : バックアップ保持期間（時間） - `8` ~ `720`

#### Gremlin データベース設定（`gremlin_database`）

-   `gremlin_database.throughput` : Gremlin データベースのスループット - `400` ~ `1000000`
-   `gremlin_database.enable_autoscale` : 自動スケーリングを有効にするかどうか - `true`, `false`

#### Gremlin グラフ設定（`gremlin_graph`）

-   `gremlin_graph.partition_key_version` : パーティションキーのバージョン - `1`, `2`
-   `gremlin_graph.partition_key_path` : パーティションキーのパス値
-   `gremlin_graph.throughput` : Gremlin グラフのスループット - `400` ~ `1000000`
-   `gremlin_graph.automatic` : 自動インデックス作成を有効にするかどうか - `true`, `false`
-   `gremlin_graph.indexing_mode` : インデックス作成モード - `consistent`, `lazy`, `none`
-   `gremlin_graph.included_paths` : インデックスに含めるパスの一覧
-   `gremlin_graph.excluded_paths` : インデックスから除外するパスの一覧
-   `gremlin_graph.conflict_resolution_mode` : 競合解決モード - `LastWriterWins`, `Custom`
-   `gremlin_graph.unique_key_paths` : データ整合性レイヤーを追加するための一意キーのパス一覧
-   `gremlin_graph.enable_autoscale` : 自動スケーリングを有効にするかどうか - `true`, `false`
-   `gremlin_graph.included_gremlin_database_name` : Gremlin グラフが作成される Gremlin データベース名

#### タグ

-   `tag` : リソースをカテゴリ分けするためのタグ - 最大 512 文字、`key`:`value`

---

## 参考資料

-   [Azure Cosmos DB（Microsoft）](https://learn.microsoft.com/ko-kr/azure/cosmos-db/)
-   [Cosmos DB API for Gremlin（Microsoft）](https://learn.microsoft.com/ko-kr/azure/cosmos-db/gremlin/introduction)
