---
title: Efs
sidebar_position: 0
tags: [AWS, Resource, Efs]
---

<hr />
<br />

## リソースアイコン

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/awsImg/efs-efs.webp"
    }
/>

## リソース概要

ファイルシステムを作成し、EC2 インスタンスにマウントしてデータの読み書きやファイル共有を行うことができます。

## 関連リソース

### 上位リソース

-   [**Region**](https://docs.cstudio.app/ja/docs/resource/aws/Base/Region)
-   [**Vpc**](https://docs.cstudio.app/ja/docs/resource/aws/Vpc/)

---

## リソース設定値

#### 基本設定

-   `creation_token` : ファイルシステム作成を保証するための一意な識別子
-   `availability_zone_name` : ファイルシステムを作成する AWS 可用性ゾーン
-   `enable_encrypted` : 暗号化有効化可否 - `true`, `false`
-   `throughput_mode` : スループットモード - `provisioned`, `elastic`, `bursting`
-   `provisioned_throughput_in_mibps` : プロビジョニングスループット（MiB/s）
-   `performance_mode` : パフォーマンスモード - `generalPurpose`, `maxIO`

#### ライフサイクルポリシー (`lifecycle_policy`)

-   `lifecycle_policy.transition_to_ia` : IA ストレージ移行までの期間
-   `lifecycle_policy.transition_to_primary_storage_class` : プライマリストレージ移行ポリシー

#### マウントターゲット (`mount_target_subnets`)

-   `mount_target_subnets` : マウント対象 Subnet 一覧

#### ファイルシステムポリシー

-   `file_system_policy` : JSON 形式のファイルシステムポリシー

#### バックアップおよびレプリケーション

-   `enable_backup_policy` : バックアップ有効化可否 - `true`, `false`
-   `enable_replica` : レプリケーション有効化可否 - `true`, `false`

#### レプリケーション設定 (`replication_configuration`)

-   `replication_configuration.replication_region` : レプリケーション先リージョン
-   `replication_configuration.replication_region_availability_zone_name` : レプリケーション先可用性ゾーン
-   `replication_configuration.kms_key_name` : 使用する KMS キー名

#### アクセスポイント (`access_point`)

-   `access_point.root_directory_path` : ルートディレクトリとして公開するパス
-   `access_point.posix_user_uid` : POSIX ユーザー ID
-   `access_point.posix_user_gid` : POSIX グループ ID
-   `access_point.posix_user_secondary_gids` : 補助 POSIX グループ ID 一覧
-   `access_point.root_owner_uid` : ルート所有者 UID
-   `access_point.root_owner_gid` : ルート所有者 GID
-   `access_point.root_permissions` : POSIX 権限（8 進数）

#### タグ

-   `tag` : Resource を分類するためのタグ

## References

-   [Amazon EFS（AWS）](https://docs.aws.amazon.com/ko_kr/efs/latest/ug/whatisefs.html)
-   [Amazon EFS（Terraform）](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/efs_file_system)
