---
title: Postgresql
sidebar_position: 0
tags: [AWS, Recurso, Rds, Postgresql]
---

<hr />
<br />

## Ícono del recurso

<img
    src={
        "https://cloudstudioblob.blob.core.windows.net/cloudstudio/awsImg/rds-postgresql.png"
    }
/>

## Descripción general del recurso

Al utilizar PostgreSQL en AWS para construir RDS, la operación y gestión de la base de datos se vuelve sencilla y altamente escalable. Además, RDS proporciona funciones de respaldo y restauración automáticas para la instancia de base de datos, lo que permite evitar la pérdida de datos. La seguridad de los datos también se garantiza mediante las funciones de seguridad ofrecidas por AWS.

## Recursos relacionados

### Recurso superior

-   [**Subnet**](https://docs.cstudio.app/docs/resource/aws/Vpc/Subnet)

### Recurso conectado

-   [**PostgreSQL**](https://docs.cstudio.app/docs/resource/aws/Rds/Postgresql)

## Valores de configuración del recurso

-   `database_name` : Nombre inicial de la base de datos
-   `engine_version` : Versión de PostgreSQL a utilizar en RDS
-   `instance_class` : Nivel de precios del RDS DB
-   `storage_type` : Tipo de almacenamiento de la instancia RDS - `gp2`, `io1`, `standard`
-   `allocated_storage` : Capacidad de almacenamiento de la instancia RDS (Gigabytes)
-   `max_allocated_storage` : Límite superior para la expansión automática del almacenamiento por parte de Amazon RDS
-   `multi_az_enabled` : Activar despliegue Multi-AZ - `true`, `false`
-   `username` : Nombre de usuario del RDS
-   `password` : Contraseña del RDS
-   `skip_final_snapshot` : Determina si se debe crear una instantánea final antes de eliminar la instancia DB
-   `included_subnet_names` : Nombres de los Subnet que se conectarán al grupo Subnet de RDS
-   `backup_retention_period` : Periodo de retención de los datos de respaldo
-   `storage_encrypted` : Especifica si el almacenamiento de la instancia DB está cifrado - `true`, `false`
-   `publicly_accessible` : Determina si la instancia tiene acceso público - `true`, `false`
-   `monitoring`
    -   `performance_insights_enabled` : Activar Performance Insights
-   `encryption`
    -   `storage_encrypted` : Especifica si el almacenamiento de la instancia DB está cifrado - `true`, `false`
    -   `kms_key_name` : Nombre de la clave KMS a utilizar para el cifrado
-   `backup`
    -   `backup_retention_period` : Periodo de retención de los datos de respaldo - `1`~`35`
    -   `final_snapshot_enabled` : Determina si se debe crear una instantánea final antes de eliminar la instancia DB - `true`, `false`
    -   `final_snapshot_name` : Nombre de la instantánea final

---

## Documentación de referencia

-   [Amazon RDS for PostgreSQL(Amazon RDS)(AWS)](https://aws.amazon.com/ko/rds/postgresql/)
-   [What is PostgreSQL?(AWS)](https://aws.amazon.com/ko/rds/postgresql/what-is-postgresql/)
-   [Amazon RDS for PostgreSQL 요금](https://aws.amazon.com/ko/rds/postgresql/pricing/)
