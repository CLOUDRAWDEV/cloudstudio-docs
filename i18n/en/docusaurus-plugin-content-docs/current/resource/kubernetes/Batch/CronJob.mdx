---
title: CronJob
sidebar_position: 0
tags: [Kubernetes, 리소스, Batch, CronJob]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/kubernetesImg/batch-cron_job.webp"
    }
/>

## Resource Overview

A Kubernetes resource that creates Jobs periodically according to a schedule.

## Associated Resources

### Parent Resource

-   [**Namespace**](https://docs.cstudio.app/docs/resource/kubernetes/Core/Namespace)

### Connected Targets

-   [**Config Map**](https://docs.cstudio.app/docs/resource/kubernetes/Core/ConfigMap)
-   [**Secret**](https://docs.cstudio.app/docs/resource/kubernetes/Core/Secret)

## Resource Setting Values

#### Basic Settings

-   `namespace` : Namespace where the CronJob will be deployed
-   `schedule` : Job execution schedule defined using a cron expression
-   `timezone` : Time zone used to interpret the schedule
-   `concurrency_policy` : Policy for handling concurrent Jobs (Allow / Forbid / Replace)
-   `starting_deadline_seconds` : Time limit for starting a Job if the schedule is missed
-   `successful_jobs_history_limit` : Number of successful Jobs to retain
-   `failed_jobs_history_limit` : Number of failed Jobs to retain
-   `containers` : Definition of container specifications to be executed
-   `init_containers` : Definition of pre-tasks executed before the main container
-   `volume` : Volume configuration to be mounted in the container
-   `pod_security_context` : Pod-level security context configuration
-   `image_pull_secrets` : Secret references for pulling images

---

## References

-   [CronJob(Kubernetes)](https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/)
-   [CronJob(Terraform)](https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs/resources/cron_job_v1)
