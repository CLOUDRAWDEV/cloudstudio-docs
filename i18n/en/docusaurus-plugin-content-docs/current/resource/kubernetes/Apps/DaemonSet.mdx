---
title: DaemonSet
sidebar_position: 0
tags: [Kubernetes, 리소스, Apps, Daemonset]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/kubernetesImg/apps-daemon_set.webp"
    }
/>

## Resource Overview

A DaemonSet is a Kubernetes resource that ensures all (or some) nodes run a copy of a Pod. When a node is added to the cluster, a Pod is automatically added to that node.

## Associated Resources

### Parent Resource

-   [**Namespace**](https://docs.cstudio.app/docs/resource/kubernetes/Core/Namespace)

### Connected Resources

-   [**Config Map**](https://docs.cstudio.app/docs/resource/kubernetes/Core/ConfigMap)
-   [**Persistent Volume Claim**](https://docs.cstudio.app/docs/resource/kubernetes/Core/PersistentVolumeClaim)
-   [**Service**](https://docs.cstudio.app/docs/resource/kubernetes/Core/Service)

## Resource Setting Values

#### Basic Settings

-   `namespace` : Namespace where the DaemonSet resource will be deployed
-   `annotations` : Metadata key-value pairs used by the Kubernetes system
-   `labels` : User-defined labels for identifying the DaemonSet resource
-   `template_labels` : Labels applied to the Pods to be created
-   `pod_security_context` : Pod-level security and permission settings
-   `init_containers` : Definitions of initialization containers executed before the main container
-   `containers` : Definitions of containers included in the Pod
-   `volume` : Volume definitions to be used by the Pod
-   `dns_policy` : DNS policy of the Pod
-   `enable_service_links` : Whether to automatically inject service environment variables
-   `host_network` : Whether to use the host network
-   `image_pull_secrets` : List of Secrets to use when pulling images

---

## References

-   [DaemonSet(Kubernetes)](https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/)
-   [DaemonSet(Terraform)](https://registry.terraform.io/providers/hashicorp/kubernetes/latest/docs/resources/daemonset)
