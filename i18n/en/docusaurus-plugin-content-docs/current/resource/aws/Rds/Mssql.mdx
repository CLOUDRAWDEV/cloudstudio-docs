---
title: Mssql
sidebar_position: 2
tags: [AWS, Resource, Rds, Mssql]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/awsImg/rds-mssql.webp"
    }
/>

## Resource Overview

SQL Server is a relational database management system developed by Microsoft. With Amazon RDS for SQL Server, you can easily set up, operate, and scale SQL Server deployments in the cloud. Amazon RDS enables you to deploy multiple editions of SQL Server—such as Express, Web, Standard, and Enterprise (2014, 2016, 2017, and 2019)—in minutes with cost-effective and scalable compute power. It also handles time-consuming database management tasks such as provisioning, backups, software patching, monitoring, and hardware scaling, allowing customers to focus solely on application development.

## Associated Resources

### Parent Resource

-   [**Subnet**](https://docs.cstudio.app/en/docs/resource/aws/Vpc/Subnet)

### Connected Resources

-   [**Mssql**](https://docs.cstudio.app/en/docs/resource/aws/Rds/Mssql)
-   [**SecurityGroup**](https://docs.cstudio.app/en/docs/resource/aws/Vpc/SecurityGroup)

## Resource Setting Values

#### Basic Settings

-   `engine` : Microsoft SQL Server engine type to use in RDS - `sqlserver-ex`, `sqlserver-ee`, `sqlserver-web`, `sqlserver-se`
-   `engine_version` : Microsoft SQL Server version to use in RDS
-   `instance_class` : RDS DB instance type
-   `storage_type` : Storage type of the RDS instance
-   `iops` : Number of I/O operations per second supported by the DB instance - `1000` ~ `40000`
-   `allocated_storage` : RDS instance storage capacity (GiB) - `20` ~ `65536`
-   `max_allocated_storage` : Upper limit for storage auto scaling
-   `multi_az_enabled` : Whether to enable Multi-AZ deployment - `true`, `false`
-   `username` : RDS username
-   `password` : RDS user password
-   `included_subnet_names` : List of Subnet names to attach to the RDS Subnet Group
-   `subnet_group_name` : Subnet Group name to use for RDS
-   `linked_security_group_names` : List of security groups to apply
-   `publicly_accessible` : Whether to allow public access - `true`, `false`

#### Monitoring Settings (`monitoring`)

-   `monitoring.performance_insights_enabled` : Whether to enable Performance Insights - `true`, `false`
-   `monitoring.kms_key_name` : KMS Key name to use for Performance Insights

#### Encryption Settings (`encryption`)

-   `encryption.storage_encrypted` : Whether to encrypt the DB instance - `true`, `false`
-   `encryption.kms_key_name` : KMS Key name to use for encryption

#### Backup Settings (`backup`)

-   `backup.backup_retention_period` : Backup retention period - `1` ~ `35`
-   `backup.final_snapshot_enabled` : Whether to create a final snapshot before DB deletion - `true`, `false`
-   `backup.final_snapshot_name` : Final snapshot name

#### Tags

-   `tag` : Tags used to categorize resources - up to 512 characters, `key`:`value`

---

## References

-   [Amazon RDS for SQL Server (AWS)](https://aws.amazon.com/ko/rds/sqlserver/)
-   [Amazon RDS for SQL Server Pricing (AWS)](https://aws.amazon.com/ko/rds/sqlserver/pricing/?nc=sn&loc=4)
