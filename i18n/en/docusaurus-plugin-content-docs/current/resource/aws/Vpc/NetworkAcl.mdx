---
title: NetworkAcl
sidebar_position: 9
tags: [AWS, Resource, Vpc, NetworkAcl]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/awsImg/vpc-network_acl.webp"
    }
/>

## Resource Overview

A Network Access Control List (ACL) allows or denies specific inbound or outbound traffic at the subnet level. You can use the default network ACL for a VPC or create a custom network ACL using rules similar to security group rules to add an additional layer of security to your VPC.  
There is no additional charge for using network ACLs.

## Associated Resources

### Parent Resource

-   [**Vpc**](https://docs.cstudio.app/en/docs/resource/aws/Vpc/)

### Connected Resources

-   [**Subnet**](https://docs.cstudio.app/en/docs/resource/aws/Vpc/Subnet)

## Resource Setting Values

#### Basic Settings

-   `included_vpc_name` : Name of the VPC where the ACL will be created
-   `nacl_description` : Description of the ACL

#### Inbound Rules (`inbound_rule`)

-   `inbound_rule.priority` : Inbound rule priority - `0` ~ `199`
-   `inbound_rule.protocol` : Inbound traffic protocol - `TCP`, `UDP`, `ICMP`
-   `inbound_rule.rule_action` : Whether to allow inbound traffic - `ALLOW`, `DROP`
-   `inbound_rule.ip_block` : Source CIDR range for inbound traffic
-   `inbound_rule.port_range` : Port range to apply the inbound rule - `1` ~ `65535`

#### Outbound Rules (`outbound_rule`)

-   `outbound_rule.priority` : Outbound rule priority - `0` ~ `199`
-   `outbound_rule.protocol` : Outbound traffic protocol - `TCP`, `UDP`, `ICMP`
-   `outbound_rule.rule_action` : Whether to allow outbound traffic - `ALLOW`, `DROP`
-   `outbound_rule.ip_block` : Destination CIDR range for outbound traffic
-   `outbound_rule.port_range` : Port range to apply the outbound rule - `1` ~ `65535`

#### Tags

-   `tag` : Tags used to categorize resources

---

## References

-   [Control traffic to subnets using Network ACLs(AWS)](https://docs.aws.amazon.com/ko_kr/vpc/latest/userguide/vpc-network-acls.html)
-   [aws_network_acl(Terraform)](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/network_acl)
