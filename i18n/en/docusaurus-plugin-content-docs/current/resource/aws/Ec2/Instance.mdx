---
title: Instance
sidebar_position: 2
tags: [AWS, 리소스, Ec2, Instance]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/awsImg/ec2-instance.webp"
    }
/>

## Resource Overview

Amazon Elastic Compute Cloud (Amazon EC2) provides scalable computing capacity in the Amazon Web Services (AWS) cloud. Amazon EC2 eliminates the need to invest in hardware upfront so you can develop and deploy applications faster. With Amazon EC2, you can launch as many virtual servers as you need, configure security and networking, and manage storage. Amazon EC2 allows you to scale up or down to handle changes in requirements or usage spikes, reducing the need to forecast traffic.

## Associated Resources

### Parent Resource

-   [**Subnet**](https://docs.cstudio.app/docs/resource/aws/Vpc/Subnet)

### Connected Resources

-   [**NetworkInterface**](https://docs.cstudio.app/docs/resource/aws/Ec2/NetworkInterface)
-   [**RouteTable**](https://docs.cstudio.app/docs/resource/aws/Vpc/RouteTable)
-   [**LbTargetGroup**](https://docs.cstudio.app/docs/resource/aws/Lb/LbTargetGroup)

## Resource Setting Values

#### Basic Settings

-   `ami` : Image that contains the information required to launch an instance (provided and supported by AWS)
-   `instance_type` : Type of the EC2 instance
-   `key_pair_name` : Name of the Key Pair to use for the EC2 instance
-   `linked_network_interface_names` : List of network interface names to use for the EC2 instance
-   `http_endpoint` : Whether the metadata service is enabled - `enabled`, `disabled`
-   `http_tokens` : Whether session tokens are required for the metadata service - `required`, `optional`
-   `cloudinit_config` : Script content to run automatically when the server is created

#### Root Volume (`root_volume`)

-   `root_volume.volume_type` : Type of the root volume - `gp2`, `gp3`, `io1`, `io2`, `sc1`, `st1`, `standard`
-   `root_volume.volume_size` : Size of the root volume (GiB)
-   `root_volume.enabled_volume_encryption` : Whether to enable encryption for the root volume - `true`, `false`

#### EBS Block Device (`ebs_block_device`)

-   `ebs_block_device.volume_type` : Type of the EBS volume - `gp2`, `gp3`, `io1`, `io2`, `sc1`, `st1`, `standard`
-   `ebs_block_device.volume_size` : Size of the EBS volume (GiB)
-   `ebs_block_device.enabled_volume_encryption` : Whether to enable encryption for the EBS volume - `true`, `false`

#### Tags

-   `tag` : Tags used to categorize resources

---

## References

-   [Amazon Ec2(AWS)](https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/concepts.html)
-   [Amazon Ec2 가격(AWS)](https://aws.amazon.com/ko/ec2/pricing/)
-   [Amazon Ec2(Terraform)](https://registry.terraform.io/providers/hashicorp/aws/latest/docs/resources/ebs_default_kms_key)
