---
title: WebApplicationFirewallPolicy
sidebar_position: 15
tags: [Azure, Network, WebApplicationFirewallPolicy, WAF]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/azureImg/network-web_application_firewall_policy.webp"
    }
/>

## Resource Overview

Web Application Firewall Policy is a **web firewall policy** applied to services such as Application Gateway (WAF), and centrally manages detection and blocking rules based on OWASP rule sets.  
By using managed rule groups (OWASP / Bot Manager) and exclusion settings, it allows you to reduce false positives while adjusting the level of protection against web attacks such as SQL injection and XSS.

## Related Resources

### Parent Resource

-   [**Resource Group**](https://docs.cstudio.app/en/docs/resource/azure/Base/ResourceGroup)

### Connected Resources

-   [**Application Gateway**](https://docs.cstudio.app/en/docs/resource/azure/Network/ApplicationGateway)

## Resource Configuration Values

-   `owasp_version` : Version of the rule set (OWASP)
-   `microsoft_bot_manager_rule_set_version` : Version of the rule set (Microsoft Bot Manager)
-   `tag` : Tags used to classify the resource

#### Managed Rule Group (`managed_rule_group`)

-   `managed_rule_group.type` : Type of the rule set - `OWASP`, `Microsoft_BotManagerRuleSet`
-   `managed_rule_group.rule_group_name` : Name of the rule group to use
-   `managed_rule_group.disabled_rules` : List of rule IDs to disable

#### Exclusion (`exclusion`)

-   `exclusion.match_variable` : Variable to match against - `RequestArgNames`, `RequestCookieNames`, `RequestHeaderNames`
-   `exclusion.selector` : Value to compare against the match variable using an operator
-   `exclusion.selector_match_operator` : Operator used to evaluate the match - `Contains`, `EndsWith`, `Equals`, `EqualsAny`, `StartsWith`

---

## References

-   [Web Application Firewall Policy Overview (Microsoft)](https://learn.microsoft.com/ko-kr/azure/web-application-firewall/ag/ag-overview)
-   [WAF Managed Rules and Exclusions (Microsoft)](https://learn.microsoft.com/ko-kr/azure/web-application-firewall/ag/application-gateway-crs-rulegroups-rules)
