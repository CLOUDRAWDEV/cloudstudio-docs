---
title: ApplicationGateway
sidebar_position: 13
tags: [Azure, Network, ApplicationGateway, LoadBalancer, WAF]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/azureImg/network-application_gateway.webp"
    }
/>

## Resource Overview

Application Gateway is an Azure-managed load balancer that performs **L7 (application layer) load balancing and routing** for web applications based on HTTP/HTTPS traffic.  
It enhances both security and scalability of web services through features such as path-based routing, SSL termination, and WAF integration.

## Associated Resources

### Parent Resource

-   [**Subnet**](https://docs.cstudio.app/docs/resource/azure/Network/Subnet)

### Connected Resources

-   [**Linux App Service**](https://docs.cstudio.app/docs/resource/azure/AppService/LinuxAppService)
-   [**Windows App Service**](https://docs.cstudio.app/docs/resource/azure/AppService/WindowsAppService)
-   [**Linux Virtual Machine**](https://docs.cstudio.app/docs/resource/azure/Compute/LinuxVirtualMachine)
-   [**Windows Virtual Machine**](https://docs.cstudio.app/docs/resource/azure/Compute/WindowsVirtualMachine)
-   [**Web Application Firewall Policy**](https://docs.cstudio.app/docs/resource/azure/Network/WebApplicationFirewallPolicy)
-   [**Public Ip**](https://docs.cstudio.app/docs/resource/azure/Network/PublicIp)

## Resource Setting Values

-   `included_subnet_name` : Name of the Subnet where the Application Gateway will be deployed
-   `linked_waf_policy_name` : Name of the Web Application Firewall Policy to link
-   `sku_tier` : Pricing tier of the Application Gateway
-   `sku_name` : SKU name of the Application Gateway
-   `enabled_autoscaling` : Whether to enable traffic-based autoscaling
-   `instance_count` : Number of instances (V1 SKU only)
-   `min_instance_count` : Minimum number of instances for autoscaling
-   `max_instance_count` : Maximum number of instances for autoscaling
-   `gateway_ip_config_name` : Name of the Gateway IP Configuration
-   `frontend_ip_address_type` : Frontend IP type - `Public`, `Private`, `Both`
-   `tag` : Tags for categorizing resources

#### Public Frontend IP Configuration (`public_frontend_ip_configuration`)

-   `public_frontend_ip_configuration.ip_config_name` : Name of the Public Frontend IP configuration
-   `public_frontend_ip_configuration.linked_public_ip_name` : Name of the Public IP to link

#### Private Frontend IP Configuration (`private_frontend_ip_configuration`)

-   `private_frontend_ip_configuration.ip_config_name` : Name of the Private Frontend IP configuration
-   `private_frontend_ip_configuration.private_ip_address_allocation` : Private IP allocation method - `Static`, `Dynamic`
-   `private_frontend_ip_configuration.private_ip_address` : Private IP address

#### Backend Pool (`backend_pool`)

-   `backend_pool.target.target_type` : Backend target type - `ip`, `fqdn`, `vm`, `app_service`
-   `backend_pool.target.target` : Backend target IP or FQDN
-   `backend_pool.target.linked_target_name` : Name of the backend resource to link

#### HTTP Listener (`http_listener`)

-   `http_listener.frontend_ip_config` : Frontend IP configuration type to use
-   `http_listener.frontend_port_name` : Name of the Frontend Port
-   `http_listener.frontend_port` : Frontend Port number
-   `http_listener.protocol` : Protocol between client and gateway - `Http`, `Https`
-   `http_listener.cert_name` : SSL certificate name when using HTTPS
-   `http_listener.pfx_cert_data` : PFX certificate data
-   `http_listener.password` : PFX certificate password

#### Backend HTTP Settings (`backend_http_settings`)

-   `backend_http_settings.protocol` : Protocol used when forwarding to backend
-   `backend_http_settings.port` : Backend listening port
-   `backend_http_settings.cookie_based_affinity` : Whether to enable cookie-based session affinity
-   `backend_http_settings.request_timeout` : Request timeout (seconds)

#### Request Routing Rule (`request_routing_rule`)

-   `request_routing_rule.http_listener_name` : Name of the HTTP Listener to link
-   `request_routing_rule.backend_pool_name` : Name of the Backend Pool to link
-   `request_routing_rule.backend_http_settings_name` : Name of the Backend HTTP Settings to link
-   `request_routing_rule.priority` : Routing rule priority

#### Path-Based Routing (`request_routing_rule.path_based_routing`)

-   `path_based_routing.paths` : List of paths
-   `path_based_routing.backend_pool_name` : Backend Pool name to link for the path
-   `path_based_routing.backend_http_settings_name` : Backend HTTP Settings name to link for the path

---

## References

-   [Azure Application Gateway Overview(Microsoft)](https://learn.microsoft.com/ko-kr/azure/application-gateway/overview)
-   [Application Gateway Components(Microsoft)](https://learn.microsoft.com/ko-kr/azure/application-gateway/application-gateway-components)
-   [WAF and Application Gateway Integration(Microsoft)](https://learn.microsoft.com/ko-kr/azure/web-application-firewall/ag/ag-overview)
