---
title: CosmosdbTable
sidebar_position: 4
tags: [Azure, CosmosdbTable, CosmosDB, Table, Database]
---

<hr />
<br />

## Resource Icon

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/azureImg/cosmosdb-cosmosdb_table.webp"
    }
/>

## Resource Overview

Azure Cosmos DB Table API is a highly available table API that provides automatic indexing without requiring separate index management.  
It offers scalability and reliability through global distribution, automatic failover, backups, and consistency policies.

## Related Resources

### Parent Resources

-   [**Resource Group**](https://docs.cstudio.app/en/docs/resource/azure/Base/ResourceGroup)

### Connected Resources

-   [**Private EndPoint**](https://docs.cstudio.app/en/docs/resource/azure/Network/PrivateEndpoint)

## Resource Configuration Values

#### Basic Settings

-   `capabilities_list` : List of Cosmos DB APIs to be used
-   `enable_multiple_write_locations` : Whether to enable multi-region writes - `true`, `false`
-   `public_network_access_enabled` : Whether to allow public access to the account via public IP addresses or service endpoints - `true`, `false`
-   `enable_automatic_failover` : Whether to enable automatic failover - `true`, `false`
-   `virtual_network_filter_enabled` : Whether to restrict access to only specific subnets in a virtual network - `true`, `false`
-   `network_acl_bypass_for_azure_services` : Whether to allow ACL bypass for Azure services - `true`, `false`

#### Region Settings (`geo_location`)

-   `geo_location.failover_priority` : Failover priority of the region
-   `geo_location.zone_redundant` : Whether to enable zone redundancy for fault tolerance - `true`, `false`

#### Consistency Policy (`consistency_policy`)

-   `consistency_policy.consistency_level` : Consistency level for configuring database availability and performance
-   `consistency_policy.max_interval_in_seconds` : Maximum allowed staleness interval (seconds) when using Bounded Staleness
-   `consistency_policy.max_staleness_prefix` : Maximum allowed number of stale requests when using Bounded Staleness

#### Backup Settings (`backup`)

-   `backup.type` : Backup policy type - `Continuous`, `Periodic`
-   `backup.interval_in_minutes` : Backup frequency in minutes - `60` ~ `1440`
-   `backup.retention_in_hours` : Backup retention period in hours - `8` ~ `720`

#### Table Settings (`table`)

-   `table.throughput` : Throughput of the Cosmos DB table - `400` ~ `1000000`
-   `table.enable_autoscale` : Whether to enable autoscale - `true`, `false`

#### Tags

-   `tag` : Tags used to categorize the resource - up to 512 characters, `key`:`value`

---

## References

-   [Azure Cosmos DB (Microsoft)](https://learn.microsoft.com/ko-kr/azure/cosmos-db/)
-   [Azure Cosmos DB Table API (Microsoft)](https://learn.microsoft.com/ko-kr/azure/cosmos-db/table/introduction)
