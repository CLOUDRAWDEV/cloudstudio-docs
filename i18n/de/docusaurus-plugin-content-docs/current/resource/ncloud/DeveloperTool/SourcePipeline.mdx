---
title: SourcePipeline
sidebar_position: 4
tags: [Ncloud, 리소스, DeveloperTool, SourcePipeline]
---

<hr />
<br />

## 리소스 아이콘

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/ncloudImg/developer_tool-source_pipeline.webp"
    }
/>

## 리소스 개요

A process automation service for fast and stable software releases by integrating SourceCommit, SourceBuild, and SourceDeploy services.

## 연관 리소스

### 상위 리소스

-   [**Region**](https://docs.cstudio.app/docs/resource/ncloud/Base/Region)

### 하위 대상

-   [**Source Build**](https://docs.cstudio.app/docs/resource/ncloud/DeveloperTool/SourceBuild)
-   [**Source Commit**](https://docs.cstudio.app/docs/resource/ncloud/DeveloperTool/SourceCommit)
-   [**Source Deploy**](https://docs.cstudio.app/docs/resource/ncloud/DeveloperTool/SourceDeploy)

## 리소스 설정 값

#### 기본 설정

-   `description` : Description of the pipeline project to be created - up to 500 characters

#### 기본 작업 (`default_task`)

-   `default_task.name` : Name of the first task executed by the pipeline - alphanumeric/`-`/`_`, 1~50 characters
-   `default_task.type` : Task type - `SourceBuild`, `SourceDeploy`
-   `default_task.target_source_build_project` : Name of the SourceBuild project to connect
-   `default_task.target_source_deploy_project` : Name of the SourceDeploy project to connect
-   `default_task.target_source_deploy_project_stage` : Stage name of the SourceDeploy project
-   `default_task.target_source_deploy_project_scenario` : Scenario name of the SourceDeploy project
-   `default_task.repository_branch` : Repository branch name for the SourceBuild task

#### 추가 작업 (`additional_task`)

-   `additional_task.type` : Task type - `SourceBuild`, `SourceDeploy`
-   `additional_task.target_source_build_project` : Name of the SourceBuild project to connect
-   `additional_task.target_source_deploy_project` : Name of the SourceDeploy project to connect
-   `additional_task.target_source_deploy_project_stage` : Stage name of the SourceDeploy project
-   `additional_task.target_source_deploy_project_scenario` : Scenario name of the SourceDeploy project
-   `additional_task.repository_branch` : Repository branch name for the SourceBuild task
-   `additional_task.pre_tasks` : List of preceding tasks to execute

#### Push 트리거 (`trigger_push_repository`)

-   `trigger_push_repository.type_of_repository` : Repository type for the trigger - `sourcecommit`
-   `trigger_push_repository.name_of_repository` : Repository name for the trigger
-   `trigger_push_repository.branch_of_repository` : Repository branch name for the trigger

#### 스케줄 트리거 (`trigger_schedule`)

-   `trigger_schedule.day` : Scheduled deployment day - `MON`, `TUE`, `WED`, `THU`, `FRI`, `SAT`, `SUN`
-   `trigger_schedule.time` : Scheduled deployment time
-   `trigger_schedule.timezone` : Scheduled deployment timezone
-   `trigger_schedule.execute_only_with_change` : Whether to execute only when there is a pipeline or SourceCommit change - `true`, `false`

#### 파이프라인 체이닝 (`trigger_target_sourcecpipeline_id`)

-   `trigger_target_sourcecpipeline_id` : List of pipeline IDs to be automatically executed after the current pipeline execution is completed

---

## 참고 자료

-   [Source Pipeline(Ncloud)](https://www.ncloud.com/product/devTools/sourcePipeline)
-   [Source Pipeline Project(Terraform)](https://registry.terraform.io/providers/NaverCloudPlatform/ncloud/latest/docs/resources/sourcepipeline_project)
