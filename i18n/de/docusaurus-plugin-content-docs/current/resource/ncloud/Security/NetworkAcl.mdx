---
title: NetworkAcl
sidebar_position: 1
tags: [Ncloud, 리소스, Security, NetworkAcl]
---

<hr />
<br />

## 리소스 아이콘

<img
    src={
        "https://cloudstudioblobprod.blob.core.windows.net/cloudstudio/ncloudImg/security-network_acl.webp"
    }
/>

## 리소스 개요

A service that enables network security management for subnets.

## 연관 리소스

### 상위 리소스

-   [**Vpc**](https://docs.cstudio.app/docs/resource/ncloud/Networking/Vpc)

### 연결 대상

-   [**Subnet**](https://docs.cstudio.app/docs/resource/ncloud/Networking/Subnet)

## 리소스 설정 값

#### 기본 설정

-   `included_vpc_name` : Name of the VPC in which to create the ACL
-   `nacl_description` : Description of the ACL

#### 인바운드 규칙 (`inbound_rule`)

-   `inbound_rule.priority` : Inbound rule priority - `0` ~ `199`
-   `inbound_rule.protocol` : Inbound traffic protocol - `TCP`, `UDP`, `ICMP`
-   `inbound_rule.rule_action` : Whether to allow inbound traffic - `ALLOW`, `DROP`
-   `inbound_rule.ip_block` : Source IP range for inbound traffic (CIDR format)
-   `inbound_rule.port_range` : Port range to apply the inbound rule - `1` ~ `65535`

#### 아웃바운드 규칙 (`outbound_rule`)

-   `outbound_rule.priority` : Outbound rule priority - `0` ~ `199`
-   `outbound_rule.protocol` : Outbound traffic protocol - `TCP`, `UDP`, `ICMP`
-   `outbound_rule.rule_action` : Whether to allow outbound traffic - `ALLOW`, `DROP`
-   `outbound_rule.ip_block` : Destination IP range for outbound traffic (CIDR format)
-   `outbound_rule.port_range` : Port range to apply the outbound rule - `1` ~ `65535`

---

## 참고 자료

-   [Network ACL(Ncloud)](https://guide.ncloud-docs.com/docs/vpc-nacl-vpc)
-   [Network ACL(Terraform)](https://registry.terraform.io/providers/NaverCloudPlatform/ncloud/latest/docs/resources/network_acl)
