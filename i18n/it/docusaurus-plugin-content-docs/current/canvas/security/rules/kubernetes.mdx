---
title: Kubernetes
sidebar_position: 4
tags: [Security, Diagnosi di sicurezza, Canvas]
---

import styles from "/src/scss/_markdown.module.scss"
import { kubernetesRules } from "/src/resource/kubernetesRules"
import { iso, csap, isms, evaluation } from "/src/resource/rules"

<hr />
<br />

## Voci di diagnosi Kubernetes

<div className={styles.securityTable}>
    <table>
        <thead>
            <tr>
                <th>Categoria</th>
                <th>Risorsa</th>
                <th>Opzione</th>
                <th>Elemento</th>
                <th>Descrizione</th>
                <th>Gravità</th>
                <th>ISO27001</th>
                <th>CSAP</th>
                <th>ISMS-P</th>
                <th>Valutazione della stabilità</th>
            </tr>
        </thead>
        <tbody>
            {kubernetesRules.map((item) => {
                return (
                    <tr>
                        <td>{item.category}</td>
                        <td>{item.resource}</td>
                        <td>{item.option}</td>
                        <td>{item.type}</td>
                        <td>{item.description}</td>
                        <td>
                            <div className={styles.tag} data-type={item.risk}>
                                {item.risk.toUpperCase()}
                            </div>
                        </td>
                        <td>
                            {item.iso} {iso[item.iso]}
                        </td>
                        <td>
                            {item.csap} {csap[item.csap]}
                        </td>
                        <td>
                            {item.isms} {isms[item.isms]}
                        </td>
                        <td>
                            {item.evaluation} {evaluation[item.evaluation]}
                        </td>
                    </tr>
                )
            })}
        </tbody>
    </table>
</div>
